export interface Frontmatter {
  date: string;
  tags: string[];
}

export interface PhotoMetadata {
  city?: string;
  state?: string;
  country?: string;
  focalLength?: string;
  exposure?: string;
  aperture?: string;
  iso?: string;
  cameraModel?: string;
  date?: string;
}
